import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.haiderrahman.cakepublisher',
  appName: 'Cake Publisher',
  webDir: 'out'
};

export default config;
